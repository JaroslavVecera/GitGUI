<UserControl x:Class="GitGUI.CommitEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitGUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center">
        <StackPanel Orientation="Horizontal" >
            <CheckBox IsChecked="{Binding Path=IsChecked}"></CheckBox>
            <TextBlock>SelectAll</TextBlock>
        </StackPanel>
        <TextBlock Margin="0,50,0,0">Commit message</TextBlock>
        <TextBox Text="{Binding Path=Message}"></TextBox>
        <Button Margin="0,10,0,0" Width="50" Command="{Binding Path=Commit}">Commit</Button>
    </StackPanel>
</UserControl>
