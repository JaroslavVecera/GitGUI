<UserControl x:Class="GitGUI.BranchLabelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitGUI"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="180">
    <Grid>
        <Border CornerRadius="2" Height="40" Background="#003a8c" Width="150">
            <TextBlock Text="{Binding Name}"
                   TextTrimming="CharacterEllipsis" 
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Padding="10,0,10,0" Foreground="White">
                <TextBlock.ToolTip>
                    <AccessText Text="{Binding Name}"/>
                </TextBlock.ToolTip>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
